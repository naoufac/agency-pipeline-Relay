# Architect: Kimi K3 — reads large context, reasons, and plans
model: openrouter/moonshotai/kimi-k3
architect: true
# Editor/coder: MiniMax M3 — writes the patch files
editor-model: openrouter/minimax/minimax-m3
# Enable OpenRouter prefix caching to drastically reduce costs on large codebase reads
cache-prompts: true
cache-keepalive-pings: 6
