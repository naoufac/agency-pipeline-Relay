{
  "name": "agency-pipeline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Autonomous agency pipeline: brief -> planner -> dependency DAG of AI calls, stage-by-stage, zero-trust verification, Supabase/Postgres board.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "demo": "tsx src/demo.ts",
    "run": "tsx src/run.ts",
    "serve": "tsx src/server.ts",
    "kpi": "tsx src/kpi-cli.ts",
    "theme:check": "tsx src/theme-check.ts",
    "dogfood": "tsx src/dogfood-cli.ts",
    "mail:test": "tsx src/mail-cli.ts"
  },
  "dependencies": {
    "@types/nodemailer": "^8.0.1",
    "nodemailer": "^9.0.1",
    "pg": "^8.13.1",
    "playwright": "^1.61.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/pg": "^8.11.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
